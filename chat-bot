<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>AI ChatBot</title>
  <style>
    body {
      background-color: #0d1117;
      color: #e6edf3;
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }
    h1 {
      color: #58a6ff;
    }
    #chatbox {
      width: 90%;
      max-width: 500px;
      height: 400px;
      border: 1px solid #30363d;
      border-radius: 10px;
      padding: 10px;
      overflow-y: auto;
      background-color: #161b22;
    }
    .message {
      margin: 10px 0;
      padding: 10px;
      border-radius: 10px;
    }
    .user {
      background-color: #238636;
      text-align: right;
    }
    .bot {
      background-color: #30363d;
      text-align: left;
    }
    #userInput {
      width: 80%;
      padding: 10px;
      border-radius: 5px;
      border: none;
      outline: none;
    }
    #sendBtn {
      padding: 10px 15px;
      background-color: #58a6ff;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }
    #sendBtn:hover {
      background-color: #1f6feb;
    }
  </style>
</head>
<body>
  <h1>💬 AI ChatBot</h1>
  <div id="chatbox"></div>
  <div style="margin-top: 10px;">
    <input type="text" id="userInput" placeholder="Type your message..." />
    <button id="sendBtn">Send</button>
  </div>

  <script>
    const chatbox = document.getElementById("chatbox");
    const userInput = document.getElementById("userInput");
    const sendBtn = document.getElementById("sendBtn");

    sendBtn.addEventListener("click", () => {
      const userText = userInput.value.trim();
      if (userText === "") return;
      appendMessage(userText, "user");
      userInput.value = "";

      // fake bot reply (demo purpose)
      setTimeout(() => {
        const botReply = "🤖: " + getBotReply(userText);
        appendMessage(botReply, "bot");
      }, 1000);
    });

    function appendMessage(text, sender) {
      const msgDiv = document.createElement("div");
      msgDiv.classList.add("message", sender);
      msgDiv.innerText = text;
      chatbox.appendChild(msgDiv);
      chatbox.scrollTop = chatbox.scrollHeight;
    }

    function getBotReply(input) {
      if (input.toLowerCase().includes("hello")) return "Hi there! How can I help you today?";
      if (input.toLowerCase().includes("name")) return "I'm your AI Assistant!";
      if (input.toLowerCase().includes("bye")) return "Goodbye! Have a great day!";
      return "I'm not sure I understand. Try asking something else!";
    }
  </script>
</body>
</html>
